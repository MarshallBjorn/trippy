spring.application.name=trippyapi

# Profil domyślny dla lokalnego developmentu.
# Nadpisywany przez env var SPRING_PROFILES_ACTIVE (patrz .env / docker-compose).
spring.profiles.default=dev
spring.datasource.url=jdbc:postgresql://db:5432/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# DB oraz Flywey
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.jpa.hibernate.ddl-auto=none
spring.flyway.baseline-on-migrate=true

# SpringBoot config
spring.jpa.open-in-view=false
spring.docker.compose.enabled=false
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# Hot Reaload
spring.devtools.restart.poll-interval=1s
spring.devtools.restart.quiet-period=500ms

# --- JWT Configuration ---
application.security.jwt.expiration=3600000
application.security.jwt.refresh-token.expiration=604800000

# Pliki
app.upload.dir=uploads
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
